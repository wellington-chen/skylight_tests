Instructions
  * You need an environment variable SKYLIGHT_USERNAME and SKYLIGHT_PASSWORD set locally. Alternatively for testing, just hard code the values.
  * Make sure you have Playwright @ latest installed
  * CD into skylight_tests and run `npx playwright test --headed --project=chromium`
